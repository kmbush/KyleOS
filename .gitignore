# Dependencies
node_modules/

# Build output
dist/
apps/web/dist/

# Runtime config and content — never committed (ADR-002, ADR-006)
config.json
content.json

# Environment values — never committed (ADR-006)
.env*
terraform.tfvars
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# Python artifacts (Lambda handlers + tests)
__pycache__/
*.pyc
.venv/
build/

# Test artifacts
coverage/
playwright-report/
test-results/

# Editor / OS
.DS_Store
*.local
